import {Component} from 'angular2/core';

@Component({
  selector: 'dashboard',
  template: `<div>
    <h2>this is the dashboard!</h2>
  </div>`
})

export default class Dashboard {
  constructor() {}
}
